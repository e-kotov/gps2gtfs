% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs_import.R
\name{g2g_terminals_from_gtfs}
\alias{g2g_terminals_from_gtfs}
\title{Derive Trip Terminals from a Static GTFS Feed}
\usage{
g2g_terminals_from_gtfs(gtfs, route_id)
}
\arguments{
\item{gtfs}{A GTFS feed object (named list of data.frames, as returned by
\code{gtfsio::import_gtfs()} or \code{gtfstools::read_gtfs()}) or a path
to a GTFS zip file (requires the 'gtfsio' package).}

\item{route_id}{A single route identifier present in \code{trips.txt}. The
gps2gtfs pipeline models one route (two terminals) at a time.}
}
\value{
A data.table with columns \code{terminal_id}, \code{latitude},
  \code{longitude}.
}
\description{
Determines the two terminals of a route from a planned (baseline) GTFS
feed: the two most frequent trip endpoints (first/last stop per trip in
\code{stop_times.txt}). The result feeds directly into
\code{terminals_data} of \code{\link{g2g_extract_trips}} and
\code{\link{g2g_extract_trips_and_stop_times}}.

Frequency ranking cannot tell two ends of a line from two platforms of one
place, so the function warns when fewer than 90\% of trip endpoints match
the two it derived (variants or branches) and when the two are close enough
together to be the same physical terminal under platform-level
\code{stop_id}s. Loop routes have no two terminals at all and error; use
\code{segmentation = "layover"} for those.
}
\examples{
gtfs <- list(
  trips = data.frame(trip_id = c("t1", "t2"), route_id = "r1"),
  stop_times = data.frame(
    trip_id = c("t1", "t1", "t2", "t2"),
    stop_id = c("A", "B", "B", "A"),
    stop_sequence = c(1, 2, 1, 2)
  ),
  stops = data.frame(
    stop_id = c("A", "B"),
    stop_lat = c(7.29, 7.31),
    stop_lon = c(80.63, 80.65)
  )
)
g2g_terminals_from_gtfs(gtfs, route_id = "r1")
}
